import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  mainContainer: {
    paddingTop: 50,
    paddingBottom: 70,
    backgroundColor: '#FFF',
  },
  card: {
    marginBottom: 0,
    maxWidth: 'auto',
  },
  paddingTop: {
    marginTop: 52,
  },
});
